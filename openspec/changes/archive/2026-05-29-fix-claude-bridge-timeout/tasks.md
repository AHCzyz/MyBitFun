## 1. Hotfix

- [x] 1.1 In `MyBitFun/resources/claude-bridge/bridge.mjs`: at module top after `query` import, parse `BITFUN_CLAUDE_BRIDGE_FIRST_EVENT_TIMEOUT_MS` and `BITFUN_CLAUDE_BRIDGE_IDLE_TIMEOUT_MS` from env (defaults 120000, lower-bound 1000, NaN-safe via `Number.isFinite`). Replace the `for await (const msg of messages)` loop with a manual `iter.next()` loop that `Promise.race`s each step against a timeout (first call uses FIRST_EVENT, subsequent use IDLE). On timeout throw `new Error('Claude SDK <phase> timed out after <N>ms')`; the existing outer `catch` already emits `error` + `turn_end`. After timeout throw, best-effort `await iter.return?.()` to release SDK iterator resources.
- [x] 1.2 Verify — `cd MyBitFun && node --check resources/claude-bridge/bridge.mjs` → **PASS, EXIT=0**. Diff inspection: timer is `clearTimeout`'d on both happy-path (after each successful `iter.next()`) and in the catch handler before re-throwing, so no leaked timers on either branch.
