## 1. Hotfix

- [x] 1.1 In `MyBitFun/src/crates/core/src/agentic/coordination/coordinator.rs`: add `use bitfun_core_types::errors::{ai_error_detail_from_message, classify_ai_error_message, ErrorCategory};` and `bitfun_runtime_ports::PortErrorKind` to the existing runtime-ports import. Add a free function `fn classify_runtime_error(message: &str, kind: Option<&PortErrorKind>) -> ErrorCategory` per design.md D1. Replace the literal `None, None` for `error_category` / `error_detail` at the three runtime `DialogTurnFailed` sites (prompt() Err branch, TurnEnd error stop_reason fallthrough, RuntimeEvent::Error) with calls to `classify_runtime_error` + `ai_error_detail_from_message`. Bonus: dropping the unused `AgentRuntime` from the import line clears the pre-existing `unused import` warning at coordinator.rs:45.
- [x] 1.2 Verify — `cd MyBitFun && cargo check -p bitfun-core --message-format=short` → **PASS, exit 0, 16.64s, no warnings** (the `AgentRuntime` unused-import warning from prior reviews is now gone).
