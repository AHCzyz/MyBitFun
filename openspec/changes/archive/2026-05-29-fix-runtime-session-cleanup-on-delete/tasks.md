## 1. Hotfix

- [x] 1.1 In `MyBitFun/src/crates/core/src/agentic/coordination/coordinator.rs`, in `pub async fn delete_session(...)` (around line 3411): (a) prepend `cancel_active_turn_for_session(session_id, Duration::from_secs(2))` with `warn!` on Err per the cascade-path pattern; (b) remove + dispose the entry from `runtime_sessions` (`.remove()` then `slot.lock().await.take()` then `dispose().await`); (c) remove the entry from `active_turns_per_session`; (d) keep the existing `session_manager.delete_session(...).await?` and `emit_event(SessionDeleted)` calls in their current order. (e) Add a doc comment above the function per design.md §D3 marking it as the canonical entry point and warning against bypass.
- [x] 1.2 Verify — `cd MyBitFun && cargo check -p bitfun-core --message-format=short` → **EXIT=0, 16.42s, no warnings**. Search-grep `runtime_sessions.*remove` and `active_turns_per_session.*remove` should now each return 1 hit (the new cleanup) where previously both returned 0.
